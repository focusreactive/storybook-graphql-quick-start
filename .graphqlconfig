{
  "projects": {
    "storybook-graphql-kit": {
      "schemaPath": "schema.json",
      "extensions": {
        "endpoints": {
          "default": {
            "url": "${env:API_ENDPOINT}",
            "headers": {
              "Authorization": "Bearer ${env:API_TOKEN}"
            }
          }
        }
      }
    }
  }
}
